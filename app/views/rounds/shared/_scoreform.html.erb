<% if @score %>

<%= form_for @score, :url => round_score_path(@score.round_id,@score.id),remote:true do |f| %>
  <%= f.text_field :strokes, class: "form-control" %>
  <%= f.check_box :gir %>
  <%= f.check_box :fairway %>
  <%= f.submit %>
<% end %>
<% end %>

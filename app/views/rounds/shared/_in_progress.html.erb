<h1>In Progress Rounds:</h1>
<% @incomplete_rounds.each do |r| %>
<%= format_date(r.created_at) %><br>
The next hole is:  <%= r.next_hole_id %>
    <%= link_to "Show Round",round_path(:id=>r.id)%><br>
<% end %>
